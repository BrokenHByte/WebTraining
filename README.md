# Учебный проект
Проект для практики навыков по созданию сервисов в фреймворке ASP.NET Core  
Архитектура проекта разделена на 4 слоя:  
**Domain** - ядро системы. Определены классы бизнес-сущностей  
**Application** - слой реализующий бизнес логику через сценарии взаимодействия пользователя с сервером  
**Infrastructure** - слой хранит реализации интерфейсов взаимодействия с базой данных и другими внешними системами. Миграции баз данных и конфигурации для бизнес-сущностей в базе  
**Web** - слой доступа к пользовательским сценариям через контроллеры  

# Сборка и запуск
1. Клонировать репозиторий с необходимой веткой
2. Запустить сервер базы данных в docker,  
запустив из корня репозитория **docker compose up -d**  
(При появлении ошибки, убедитесь что docker установлен и сервис запущен)
3. Запустить из папки проекта команду dotnet run --project Web
4. Можно проверить по адресу
`http://localhost:5008/swagger/index.html`

- Юнит тесты проекта запускаются из папки проекта командой  
**dotnet test Application.UnitTests**
- Интеграционные тесты проекта запускаются из папки проекта командой  
**dotnet test Infrastructure.IntegrationTests**  
Для запуска интеграционных тестов должен быть запущен сервис docker
- Для поддержания актуального состояния базы данных используются миграции.  
Новую миграцию можно создать и применить командами:  
**dotnet.exe ef migrations add --project Infrastructure\Infrastructure.csproj --startup-project Web\Web.csproj --context Infrastructure.Data.AppDbContext --configuration Debug НазваниеМиграции --output-dir Data\Migrations**   
**dotnet ef database update**  
- В качестве базы данных используется postgres-16
  
# Аутентификация и авторизация  
1. Используя Swagger (/auth/register) создайте пользователя с правами "Admin" или "User"  
   (пользователи без прав администратора не могут управлять событиями и удалять чужие бронирования)
2. Авторизуйтесь в Swagger (/auth/login)  
3. Полученный в результате авторизации токен скопируйте в поле по кнопке "Authorize"  
4. Проверка результата: токен должен передаваться в Header в любых запросах к серверу    
  
Для production необходимо заменить получение ключей Jwt из конфигурации,   
на получение ключа из секретов репозитория. Либо из окружения на сервере      
  
В случае проблем с авторизацией возвращает:  
401 - если никакой пользователь не авторизован  
403 - если прав пользователь не достаточно

# Доступное API
(Формат ответа при ошибках соответсвует Problem Details (RFC 7807))

POST /auth/register  
**Тело запроса**
```json
{
  "login": "Ваш логин",
  "password": "Ваш пароль",
  "role": "Права" (Admin or User)
}
```  
**Возвращает**  
204 при успехе  
409 если указанный логин уже существует   

POST /auth/login  
**Тело запроса**
```json
{
  "login": "Ваш логин",
  "password": "Ваш пароль",
  "role": "Права" (Admin or User)
}
```  
**Возвращает**  
200 при успехе  
500 если логин или пароль не верен
 
**Пример успешного ответа:**  
```json
{
"token": "eyJhbAub3JQ6cKFwn0..."
}
```

GET /events (Доступно без авторизации)
**Параметры запроса**  
```
title 
(string, опциональный)
(регистронезависимый, частичное совпадение)
отбор по заголовку события

from 
(DateTime, опциональный)
отбор событий начинающихся до указанной даты

to 
(DateTime, опциональный)
фильтрация событий начинающихся до указанной даты

page
(int, опциональный, по умолчанию 1)
номер подгруппы в группе отобранных результатов

pageSize
(int, опциональный, по умолчанию 10)
количество событий на одну подгруппу 
```
**Пример ответа:**
```json
{
  "totalCountEvents": 4,
  "currentPage": 2,
  "pageSize": 2, // Max = pageSize
  "events": [
    {
      "id": "5decc173-b4be-4fc6-96f9-04c276290099",
      "title": "12345",
      "description": "12346",
      "startAt": "2026-05-13T13:30:10.871Z",
      "endAt": "2026-08-13T13:30:10.871Z",
	  "totalSeats": 5,
      "availableSeats": 2
    },
    {
      "id": "4fecc173-b4be-4fc6-96f9-04c276290099",
      "title": "123456",
      "description": "123467",
      "startAt": "2026-05-13T13:30:10.871Z",
      "endAt": "2026-08-13T13:30:10.871Z",
	  "totalSeats": 30,
      "availableSeats": 30
    }
  ]
}
```

------------------------
### Добавить событие
POST /events (Необходимы права администратора)  
**Тело запроса**  
```json
{
  "title": "Заголовок",
  "description": "Описание",
  "startAt": "2026-04-27T03:11:08.393Z",
  "endAt": "2026-04-27T06:11:08.393Z",
  "totalSeats": 20
}
```
**Возвращает**  
201 при успехе  
400 если есть нарушения в теле запроса

------------------------
### Получить событие по значению Id
GET /events/{id} (Доступно без авторизации)  
**Пример ответа:**
```json
{
  "id": "5decc173-b4be-4fc6-96f9-04c276290099",
  "title": "Заголовок",
  "description": "Описание",
  "startAt": "2026-04-27T03:11:08.393Z",
  "endAt": "2026-04-27T06:11:08.393Z",
  "totalSeats": 30,
  "availableSeats": 25
}
```
**Возвращает**  
200 при успехе  
404 если событие не найдено

------------------------
### Изменить событие по Id
PUT /events/{id} (Необходимы права администратора)    
**Тело запроса**
```json
{
  "title": "Новый заголовок",
  "description": "Описание",
  "startAt": "2026-04-27T03:11:08.393Z",
  "endAt": "2026-04-27T06:11:08.393Z",
  "totalSeats": 20
}
```
**Возвращает**  
200 при успехе  
404 если событие не найдено  
400 если есть нарушения в теле запроса

------------------------
### Удалить событие по Id
DELETE /events/{id} (Необходимы права администратора)    

**Возвращает**  
200 при успехе  
404 если событие не найдено

------------------------
### Бронирование
Порядок бронирования
1. Выбираем событие на основании **GET /events**
2. Бронируем событие через **POST /events/{id}/book**
3. Проверяем статус брони через **GET /bookings/{id}**

### Фоновый сервис обработки бронирования
**BookingBackgroundService**

Циклично
1. Каждые 100 мс проверяет есть ли забронированные события со статусом "Pending"
2. Выполняет задержку 2 секунды для каждой найденной брони переводя статус в состояние "Confirmed"

Синхронизация реализованна с использование SemaphoreSlim. 
Предполагается что внутри повсеместно используются async функции

Пример овербукинга:
1. Создаём событие со значением totalSeats = 3
2. Выполняем бронирование (POST /events/{id}/book) для созданного события 4 раза
3. Получаем ошибку 409 для 11ой попытки


------------------------
### Забронировать событие по id 
POST /bookings/{id}/book (Достаточно прав пользователя)  

**Пример ответа:**
```json
{
  "id": "a75f87e4-de26-4a8c-93aa-9df9dd32a3b5",
  "eventId": "5decc173-b4be-4fc6-96f9-04c276290099",
  "status": "Pending"
}
```
**Возвращает**  
202 если бронь зарегистрирована  
404 если событие не найдено  
409 если мест для бронирования в событии не осталось 

DELETE /bookings/{id} (Достаточно прав пользователя)  
(Необходимы права администратора в случае отмены чужой брони)  
(Бронь не удаляется из базы, только отменяется)
**Возвращает**   
200 если бронь отменена  
404 если бронь не найдена    
403 если прав пользователя не достаточно  

------------------------
### Проверка статуса бронирования по id брони
GET /bookings/{id} (Достаточно прав пользователя)  

**Пример ответа:**

Возвращает полную модель бронирования
```json
{
  "id": "e1351cf3-ae1f-4882-8217-49a26e484b00",
  "eventId": "c4856d85-2955-4e1c-b173-134689f03916",
  "status": "Confirmed",
  "createdAt": "2026-05-25T06:45:49.5022756Z",
  "processedAt": "2026-05-25T06:45:51.5905483Z"
}
```
Варианты status:    
Pending, Confirmed, Rejected

**Возвращает**  
200 если успешно  
404 если бронирование не найдено  
